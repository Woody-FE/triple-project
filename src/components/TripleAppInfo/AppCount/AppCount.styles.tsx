import styled from "styled-components";

import { withText } from "@common/index";

export const AppinfoText = withText({ tagName: "p", customFontSize: 36 });

export const AppInfoItem = styled.div`
  margin-bottom: 20px;
`;
